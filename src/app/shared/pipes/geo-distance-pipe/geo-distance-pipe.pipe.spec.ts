import { GeoDistancePipePipe } from './geo-distance-pipe.pipe';

describe('GeoDistancePipePipe', () => {
  it('create an instance', () => {
    const pipe = new GeoDistancePipePipe();
    expect(pipe).toBeTruthy();
  });
});
