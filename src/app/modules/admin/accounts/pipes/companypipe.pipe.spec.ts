import { CompanypipePipe } from './companypipe.pipe';

describe('CompanypipePipe', () => {
  it('create an instance', () => {
    const pipe = new CompanypipePipe();
    expect(pipe).toBeTruthy();
  });
});
