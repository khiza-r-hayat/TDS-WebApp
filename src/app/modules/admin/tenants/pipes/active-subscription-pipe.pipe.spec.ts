import { ActiveSubscriptionPipePipe } from './active-subscription-pipe.pipe';

describe('ActiveSubscriptionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveSubscriptionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
