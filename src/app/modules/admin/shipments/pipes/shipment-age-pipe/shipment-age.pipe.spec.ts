import { ShipmentAgePipe } from './shipment-age.pipe';

describe('ShipmentAgePipe', () => {
  it('create an instance', () => {
    const pipe = new ShipmentAgePipe();
    expect(pipe).toBeTruthy();
  });
});
